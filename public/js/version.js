'use strict';

// __BUILD__ is replaced with the commit + date by the Pages deploy workflow.
// When it still reads "__BUILD__", you're running an unstamped local copy.
window.VITREA_VERSION = {
  semver: '1.13.2',
  build: '__BUILD__',
};
